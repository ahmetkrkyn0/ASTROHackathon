fastapi==0.115.6
# Used DIRECTLY by app.main and app.pose (BaseModel, Field, field_validator,
# conlist), not merely pulled in by FastAPI. It arrived transitively, in a
# file where every other dependency is ==-pinned, so a FastAPI bump could
# have swapped the validation library underneath the contracts without a
# line changing here. (Round 3 review, L-14.)
pydantic==2.9.2
uvicorn==0.34.0
rasterio==1.4.3
numpy==2.2.1
scipy==1.15.0
pyproj==3.7.2
httpx>=0.27.0
pytest==8.3.4
# heat1d PyPI release (0.3.2 as of writing) has no slope/aspect support at
# all -- see backend/app/thermal_model.py:Heat1DModel docstring and
# .superpowers/sdd/2026-08-19-faz1-gercek-fizik/task-4-report.md for the
# full investigation. The GitHub main branch (0.4.1) adds slope/slope_az
# but its installable package root lives in a python/ subdirectory, hence
# the #subdirectory=python fragment below. Pinned to the commit verified
# against this codebase; resolves cleanly against numpy==2.2.1 above.
heat1d @ git+https://github.com/phayne/heat1d@6b25d0f01f015ea815704a6cd0a67af3b9fc0fb8#subdirectory=python
# heat1d's transitive deps, pinned for reproducibility (every other line in
# this file is ==-pinned; heat1d itself declares no version bounds on these,
# and a future numba release could tighten its numpy range enough to make
# this file unresolvable against numpy==2.2.1 above). Versions below are
# exactly what resolved cleanly against numpy==2.2.1 in this environment.
# astropy-iers-data (heat1d -> astropy -> astropy-iers-data) is deliberately
# left unpinned: it is date-versioned and republishes ~weekly, so pinning it
# would go stale almost immediately.
numba==0.67.0
llvmlite==0.49.0
astropy==8.0.1
spiceypy==6.0.0
